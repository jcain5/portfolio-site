// Substantially reduced from the previous four-card explainer section — the
// site already surfaces evidence-source badges on every project/competency,
// so this is now a single labeling note rather than a section that spends
// homepage space re-explaining the philosophy.
export default function EvidenceModel() {
  return (
    <div className="py-6 px-6 bg-white border-y border-border">
      <p className="container-grid text-center text-sm text-muted">
        Professional, volunteer, independent-lab, and portfolio-project work are labeled
        separately throughout the site.
      </p>
    </div>
  );
}
